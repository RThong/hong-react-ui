/**
 * title: 按钮类型
 * desc: 按钮有四种类型：默认按钮、主要按钮、虚线按钮和危险按钮。
 */
import React from 'react';
import { Button } from 'hong-ui';

const Base = () => {
  return (
    <div>
      <Button>Defaultsss!!</Button>
    </div>
  );
};

export default Base;
